<!DOCTYPE html>
<html lang="{{ request.locale.iso_code }}" class="no-js">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  <meta name="theme-color" content="#ffffff">
  <meta name="format-detection" content="telephone=no">

  {%- if page_description -%}
    <meta name="description" content="{{ page_description | escape }}">
  {%- endif -%}

  <title>
    {%- if template == 'index' -%}
      {{ shop.name }} &mdash; The Desk Bed Your Cat Actually Uses
    {%- elsif page_title -%}
      {{ page_title }} &mdash; {{ shop.name }}
    {%- else -%}
      {{ shop.name }}
    {%- endif -%}
  </title>

  {{ content_for_header }}

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400;1,600&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">

  {{ 'deskpaws.css' | asset_url | stylesheet_tag }}

  <script>document.documentElement.classList.remove('no-js');</script>
</head>

<body class="template-{{ template.name | replace: '.', '-' | downcase }}">

  {% section 'header' %}

  <main id="main-content" role="main" tabindex="-1">
    {{ content_for_layout }}
  </main>

  {% section 'footer' %}

  {% section 'buy-drawer' %}

  {%- if template.name == 'index' or template.name == 'product' -%}
  <div class="sticky-cta" id="sticky-cta" aria-hidden="true">
    <span class="sticky-cta__price">$79</span>
    <a href="{{ routes.root_url }}products/desk-paws-place-for-your-cat-co-worker" class="btn btn--primary-fill sticky-cta__btn">
      Claim My Cat&rsquo;s Spot
    </a>
  </div>
  {%- endif -%}

  {{ 'deskpaws.js' | asset_url | script_tag }}

</body>
</html>
